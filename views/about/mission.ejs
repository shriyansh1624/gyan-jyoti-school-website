<%- include('../partials/header') %>

<section class="about-section">
  <div class="container">
    <h1><%= data && data.title ? data.title : "Our Mission" %></h1>
    <div class="about-content">
      <%= data && data.content ? data.content : '<p>Mission statement goes here.</p>' %>
    </div>
  </div>
</section>

<%- include('../partials/footer') %>