<%- include('../partials/header') %>

<section class="reg-hero">
  <div class="reg-container">
    <span class="hero-badge">Online Admission</span>
    <h1>Registration Form</h1>
    <p>
      Fill the admission registration form carefully. Our admission team will review
      your application and contact you soon.
    </p>

    <div class="hero-actions">
      <a href="/admission/enquiry">
        <i class="fas fa-question-circle"></i> Admission Enquiry
      </a>
      <a href="tel:+919876543210">
        <i class="fas fa-phone"></i> Call School
      </a>
    </div>
  </div>
</section>

<section class="registration-section">
  <div class="reg-container">

    <div class="section-heading">
      <span class="section-tag">Student Admission</span>
      <h2>Online Admission Registration</h2>
      <p>
        Please enter correct student, parent and contact details. Only valid details
        will be accepted by the system.
      </p>
    </div>

    <div class="registration-layout">

      <div class="reg-info-card">
        <div class="logo-box">
          <img src="/images/logo.png" alt="Gyan Jyoti Logo">
        </div>

        <h2>Gyan Jyoti School</h2>
        <p>
          Registration is the first step towards admission. Submit correct details
          and our team will contact you for the next process.
        </p>

        <div class="process-box">
          <div><span>01</span> Fill Registration Form</div>
          <div><span>02</span> Upload Required Document</div>
          <div><span>03</span> School Team Verification</div>
          <div><span>04</span> Admission Confirmation</div>
        </div>

        <div class="help-box">
          <i class="fas fa-shield-alt"></i>
          <p>Your details are protected and used only for admission purposes.</p>
        </div>
      </div>

      <div class="registration-card">

        <% if (typeof query !== 'undefined' && query.success) { %>
          <div class="success-msg">
            <i class="fas fa-check-circle"></i>
            Registration submitted successfully. Our team will contact you soon.
          </div>
        <% } %>

        <% if (typeof query !== 'undefined' && query.error) { %>
          <div class="error-msg">
            <i class="fas fa-exclamation-circle"></i>
            Something went wrong. Please fill all details correctly and try again.
          </div>
        <% } %>

        <form action="/admission/registration" method="POST" enctype="multipart/form-data" class="registration-form" novalidate>

          <div class="form-section-title">
            <i class="fas fa-user-graduate"></i>
            <h3>Student Details</h3>
          </div>

          <div class="form-grid">

            <div class="form-group">
              <label>Student Name <span>*</span></label>
              <input
                type="text"
                name="studentName"
                placeholder="Enter student name"
                pattern="[A-Za-z\s.]{3,70}"
                maxlength="70"
                title="Only letters allowed, minimum 3 characters"
                required
              >
            </div>

            <div class="form-group">
              <label>Class Applying For <span>*</span></label>
              <input
                type="text"
                name="class"
                placeholder="Example: Class 6"
                maxlength="30"
                required
              >
            </div>

          </div>

          <div class="form-section-title">
            <i class="fas fa-user-friends"></i>
            <h3>Parent / Guardian Details</h3>
          </div>

          <div class="form-grid">

            <div class="form-group">
              <label>Parent / Guardian Name <span>*</span></label>
              <input
                type="text"
                name="parentName"
                placeholder="Enter parent / guardian name"
                pattern="[A-Za-z\s.]{3,70}"
                maxlength="70"
                title="Only letters allowed, minimum 3 characters"
                required
              >
            </div>

            <div class="form-group">
              <label>Mobile Number <span>*</span></label>
              <input
                type="text"
                name="phone"
                placeholder="10 digit mobile number"
                pattern="[0-9]{10}"
                maxlength="10"
                inputmode="numeric"
                title="Enter exactly 10 digits"
                required
              >
            </div>

          </div>

          <div class="form-grid">

            <div class="form-group">
              <label>Email Address</label>
              <input
                type="email"
                name="email"
                placeholder="Enter email address"
                maxlength="80"
              >
            </div>

            <div class="form-group">
              <label>Upload Document</label>
              <input
                type="file"
                name="document"
                accept=".pdf,.jpg,.jpeg,.png"
              >
              <small class="file-note">
                Allowed: PDF, JPG, JPEG, PNG only. Max size: 2MB.
              </small>
            </div>

          </div>

          <div class="form-section-title">
            <i class="fas fa-map-marker-alt"></i>
            <h3>Address Details</h3>
          </div>

          <div class="form-group">
            <label>Full Address <span>*</span></label>
            <textarea
              name="address"
              placeholder="Enter full address"
              minlength="5"
              maxlength="500"
              required
            ></textarea>
          </div>

          <button type="submit" class="submit-btn">
            <i class="fas fa-paper-plane"></i> Submit Registration
          </button>

        </form>

      </div>

    </div>

  </div>
</section>

<section class="registration-support">
  <div class="reg-container">

    <div class="support-grid">
      <div class="support-card">
        <i class="fas fa-file-alt"></i>
        <h3>Document Upload</h3>
        <p>Upload admission related document in PDF, JPG or PNG format.</p>
      </div>

      <div class="support-card">
        <i class="fas fa-phone-volume"></i>
        <h3>Admission Support</h3>
        <p>Our school team will call you after successful registration.</p>
      </div>

      <div class="support-card">
        <i class="fas fa-school"></i>
        <h3>School Visit</h3>
        <p>Parents can visit school office for admission confirmation.</p>
      </div>
    </div>

  </div>
</section>

<style>
/* =========================================================
   PREMIUM ADMISSION REGISTRATION
   Gyan Jyoti School — Navy / Gold Theme
   Backend contracts intentionally preserved.
========================================================= */

.reg-container{
  width:min(1180px,92%);
  margin:0 auto;
}

.reg-hero{
  min-height:590px;
  position:relative;
  display:flex;
  align-items:center;
  overflow:hidden;
  background:
    radial-gradient(circle at 78% 20%,rgba(255,195,0,.18),transparent 28%),
    linear-gradient(125deg,#000b17 0%,#001f3f 55%,#075d98 100%);
}

.reg-hero::before{
  content:"";
  position:absolute;
  inset:0;
  background:
    linear-gradient(90deg,rgba(0,8,18,.91),rgba(0,31,63,.55)),
    url('/images/school-bg.jpg') center/cover no-repeat;
  opacity:.40;
}

.reg-hero::after{
  content:"";
  position:absolute;
  inset:0;
  background:
    linear-gradient(rgba(255,255,255,.09) 1px,transparent 1px),
    linear-gradient(90deg,rgba(255,255,255,.09) 1px,transparent 1px);
  background-size:70px 70px;
  opacity:.18;
  animation:regGrid 25s linear infinite;
}

@keyframes regGrid{
  from{transform:translate(0,0)}
  to{transform:translate(-70px,-70px)}
}

.reg-hero .reg-container{
  position:relative;
  z-index:3;
}

.hero-badge,
.section-tag{
  display:inline-flex;
  align-items:center;
  gap:9px;
  padding:11px 19px;
  border-radius:50px;
  background:#ffc300;
  color:#001f3f;
  font-size:11px;
  font-weight:950;
  letter-spacing:1.25px;
  text-transform:uppercase;
  box-shadow:0 12px 30px rgba(255,195,0,.22);
}

.reg-hero h1{
  max-width:900px;
  margin:20px 0 17px;
  color:#fff;
  font-size:clamp(48px,7vw,82px);
  line-height:.98;
  letter-spacing:-3px;
  font-weight:950;
  text-shadow:0 20px 50px rgba(0,0,0,.38);
}

.reg-hero p{
  max-width:760px;
  margin:0;
  color:rgba(255,255,255,.73);
  font-size:18px;
  line-height:1.8;
}

.hero-actions{
  display:flex;
  flex-wrap:wrap;
  gap:14px;
  margin-top:32px;
}

.hero-actions a{
  display:inline-flex;
  align-items:center;
  gap:9px;
  padding:15px 23px;
  border-radius:50px;
  text-decoration:none;
  background:#ffc300;
  color:#001f3f;
  font-weight:900;
  box-shadow:0 14px 30px rgba(255,195,0,.24);
  transition:.35s;
}

.hero-actions a:nth-child(2){
  background:rgba(255,255,255,.09);
  color:#fff;
  border:1px solid rgba(255,255,255,.22);
  box-shadow:none;
}

.hero-actions a:hover{
  transform:translateY(-5px);
  background:#fff;
  color:#001f3f;
}

/* =========================================================
   MAIN REGISTRATION AREA
========================================================= */

.registration-section{
  padding:105px 0;
  background:
    radial-gradient(circle at 8% 10%,rgba(255,195,0,.08),transparent 24%),
    #f5f8fb;
}

.section-heading{
  max-width:850px;
  margin:0 auto 58px;
  text-align:center;
}

.section-heading h2{
  margin:18px 0 13px;
  color:#001f3f;
  font-size:clamp(38px,5vw,56px);
  line-height:1.04;
  letter-spacing:-1.7px;
  font-weight:950;
}

.section-heading p{
  max-width:780px;
  margin:auto;
  color:#667585;
  font-size:16px;
  line-height:1.85;
}

.registration-layout{
  display:grid;
  grid-template-columns:.82fr 1.18fr;
  gap:28px;
  align-items:start;
}

.reg-info-card,
.registration-card{
  position:relative;
  overflow:hidden;
  border-radius:32px;
  box-shadow:0 25px 70px rgba(0,31,63,.11);
}

.reg-info-card{
  position:sticky;
  top:118px;
  padding:40px;
  color:#fff;
  background:
    radial-gradient(circle at 90% 0,rgba(255,195,0,.18),transparent 30%),
    linear-gradient(145deg,#000f20,#003d6b);
  border:1px solid rgba(255,255,255,.08);
}

.registration-card{
  padding:42px;
  background:#fff;
  border:1px solid #e6edf3;
}

.reg-info-card::after,
.registration-card::after{
  content:"";
  position:absolute;
  width:280px;
  height:280px;
  right:-155px;
  top:-155px;
  border-radius:50%;
  border:1px solid rgba(255,195,0,.16);
}

.reg-info-card > *,
.registration-card > *{
  position:relative;
  z-index:2;
}

.logo-box{
  width:100px;
  height:100px;
  padding:7px;
  border:4px solid #ffc300;
  border-radius:26px;
  background:#fff;
  box-shadow:0 18px 40px rgba(0,0,0,.18);
}

.logo-box img{
  width:100%;
  height:100%;
  object-fit:contain;
}

.reg-info-card h2{
  margin:25px 0 10px;
  color:#ffc300;
  font-size:32px;
  line-height:1.1;
  font-weight:950;
}

.reg-info-card > p{
  margin:0 0 28px;
  color:rgba(255,255,255,.72);
  font-size:15px;
  line-height:1.85;
}

.process-box{
  display:grid;
  gap:11px;
  margin:26px 0;
}

.process-box div{
  display:flex;
  align-items:center;
  gap:12px;
  padding:13px;
  border:1px solid rgba(255,255,255,.10);
  border-radius:15px;
  background:rgba(255,255,255,.055);
  color:rgba(255,255,255,.82);
  font-size:13px;
  font-weight:800;
  transition:.3s;
}

.process-box div:hover{
  transform:translateX(6px);
  background:rgba(255,195,0,.09);
}

.process-box span{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  flex:0 0 34px;
  width:34px;
  height:34px;
  border-radius:50%;
  background:#ffc300;
  color:#001f3f;
  font-size:11px;
  font-weight:950;
}

.help-box{
  display:flex;
  align-items:flex-start;
  gap:13px;
  padding:16px;
  border-radius:17px;
  background:rgba(255,255,255,.08);
  border:1px solid rgba(255,255,255,.10);
}

.help-box i{
  margin-top:2px;
  color:#ffc300;
  font-size:22px;
}

.help-box p{
  margin:0;
  color:rgba(255,255,255,.73);
  font-size:12px;
  line-height:1.7;
}

/* =========================================================
   ALERTS
========================================================= */

.success-msg,
.error-msg{
  display:flex;
  align-items:flex-start;
  gap:11px;
  margin-bottom:24px;
  padding:15px 17px;
  border-radius:16px;
  font-size:14px;
  font-weight:800;
  line-height:1.6;
}

.success-msg{
  color:#0f5132;
  background:#e5f7ee;
  border:1px solid #bde7d0;
}

.error-msg{
  color:#842029;
  background:#fff0f1;
  border:1px solid #f2c6ca;
}

/* =========================================================
   FORM
========================================================= */

.form-section-title{
  display:flex;
  align-items:center;
  gap:13px;
  margin:7px 0 24px;
  padding-bottom:15px;
  border-bottom:1px solid #e8edf2;
}

.form-section-title i{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  flex:0 0 44px;
  width:44px;
  height:44px;
  border-radius:14px;
  background:#001f3f;
  color:#ffc300;
  box-shadow:0 8px 20px rgba(0,31,63,.13);
}

.form-section-title h3{
  margin:0;
  color:#001f3f;
  font-size:21px;
  font-weight:950;
}

.form-grid{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:18px;
}

.form-group{
  margin-bottom:20px;
}

.form-group label{
  display:flex;
  align-items:center;
  gap:3px;
  margin-bottom:8px;
  color:#102b43;
  font-size:13px;
  font-weight:900;
}

.form-group span{
  color:#dc2626;
}

.registration-form input,
.registration-form textarea{
  width:100%;
  border:1px solid #d9e1e8;
  border-radius:14px;
  padding:15px 16px;
  background:#fbfcfe;
  color:#172b3e;
  outline:none;
  font:inherit;
  font-size:14px;
  transition:.28s;
}

.registration-form input::placeholder,
.registration-form textarea::placeholder{
  color:#9aa7b4;
}

.registration-form input:hover,
.registration-form textarea:hover{
  border-color:#b9c7d3;
  background:#fff;
}

.registration-form input:focus,
.registration-form textarea:focus{
  border-color:#ffc300;
  background:#fff;
  box-shadow:0 0 0 4px rgba(255,195,0,.17);
}

.registration-form textarea{
  min-height:145px;
  resize:vertical;
}

.registration-form input[type="file"]{
  padding:12px;
  cursor:pointer;
}

.registration-form input[type="file"]::file-selector-button{
  margin-right:10px;
  border:0;
  padding:9px 13px;
  border-radius:9px;
  background:#001f3f;
  color:#fff;
  cursor:pointer;
  font-weight:800;
}

.file-note{
  display:block;
  margin-top:7px;
  color:#778594;
  font-size:11px;
  line-height:1.5;
}

.submit-btn{
  width:100%;
  display:flex;
  align-items:center;
  justify-content:center;
  gap:10px;
  border:0;
  margin-top:8px;
  padding:17px 25px;
  border-radius:15px;
  background:linear-gradient(135deg,#ffc300,#ffdd55);
  color:#001f3f;
  font-size:15px;
  font-weight:950;
  cursor:pointer;
  box-shadow:0 14px 30px rgba(255,195,0,.28);
  transition:.35s;
}

.submit-btn:hover{
  transform:translateY(-4px);
  box-shadow:0 20px 40px rgba(255,195,0,.38);
}

.submit-btn:disabled{
  opacity:.72;
  cursor:not-allowed;
  transform:none;
}

/* =========================================================
   SUPPORT
========================================================= */

.registration-support{
  padding:95px 0;
  background:#fff;
}

.support-grid{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:20px;
}

.support-card{
  position:relative;
  overflow:hidden;
  padding:35px 30px;
  border:1px solid #e3eaf0;
  border-radius:27px;
  background:#f7f9fb;
  text-align:center;
  box-shadow:0 16px 45px rgba(0,31,63,.06);
  transition:.4s;
}

.support-card::before{
  content:"";
  position:absolute;
  left:0;
  top:0;
  width:100%;
  height:4px;
  background:#ffc300;
}

.support-card:hover{
  transform:translateY(-10px);
  background:#001f3f;
  box-shadow:0 28px 65px rgba(0,31,63,.18);
}

.support-card i{
  display:flex;
  align-items:center;
  justify-content:center;
  width:65px;
  height:65px;
  margin:0 auto 18px;
  border-radius:19px;
  background:#001f3f;
  color:#ffc300;
  font-size:25px;
  transition:.35s;
}

.support-card:hover i{
  background:#ffc300;
  color:#001f3f;
}

.support-card h3{
  margin:0 0 10px;
  color:#001f3f;
  font-size:21px;
  font-weight:950;
}

.support-card p{
  margin:0;
  color:#667585;
  font-size:13px;
  line-height:1.75;
}

.support-card:hover h3{
  color:#fff;
}

.support-card:hover p{
  color:rgba(255,255,255,.65);
}

/* =========================================================
   RESPONSIVE
========================================================= */

@media(max-width:950px){
  .registration-layout{
    grid-template-columns:1fr;
  }

  .reg-info-card{
    position:relative;
    top:auto;
  }

  .support-grid{
    grid-template-columns:1fr 1fr;
  }
}

@media(max-width:700px){
  .reg-container{
    width:92%;
  }

  .reg-hero{
    min-height:530px;
  }

  .reg-hero h1{
    font-size:47px;
    letter-spacing:-2px;
  }

  .reg-hero p{
    font-size:15px;
  }

  .registration-section,
  .registration-support{
    padding:72px 0;
  }

  .section-heading{
    margin-bottom:40px;
  }

  .section-heading h2{
    font-size:37px;
  }

  .registration-card,
  .reg-info-card{
    padding:28px 22px;
    border-radius:25px;
  }

  .form-grid,
  .support-grid{
    grid-template-columns:1fr;
  }

  .hero-actions{
    flex-direction:column;
  }

  .hero-actions a{
    justify-content:center;
  }
}

@media(prefers-reduced-motion:reduce){
  .reg-hero::after{
    animation:none;
  }

  .hero-actions a,
  .support-card,
  .submit-btn,
  .process-box div{
    transition:none;
  }
}
</style>

<script>
  const registrationForm = document.querySelector('.registration-form');

  if (registrationForm) {
    registrationForm.addEventListener('submit', function(e) {
      const studentName = this.studentName.value.trim();
      const parentName = this.parentName.value.trim();
      const phone = this.phone.value.trim();
      const address = this.address.value.trim();
      const file = this.document.files[0];

      const nameRegex = /^[A-Za-z\s.]{3,70}$/;

      if (!nameRegex.test(studentName)) {
        alert('Student name should contain only letters and minimum 3 characters.');
        e.preventDefault();
        return;
      }

      if (!nameRegex.test(parentName)) {
        alert('Parent / Guardian name should contain only letters and minimum 3 characters.');
        e.preventDefault();
        return;
      }

      if (!/^[0-9]{10}$/.test(phone)) {
        alert('Mobile number must be exactly 10 digits.');
        e.preventDefault();
        return;
      }

      if (address.length < 5) {
        alert('Address must be at least 5 characters.');
        e.preventDefault();
        return;
      }

      if (file) {
        const allowedTypes = ['application/pdf', 'image/jpeg', 'image/png'];

        if (!allowedTypes.includes(file.type)) {
          alert('Only PDF, JPG, JPEG and PNG files are allowed.');
          e.preventDefault();
          return;
        }

        if (file.size > 2 * 1024 * 1024) {
          alert('File size must be below 2MB.');
          e.preventDefault();
          return;
        }
      }

      const btn = this.querySelector('.submit-btn');
      btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Submitting...';
      btn.disabled = true;
    });
  }
</script>

<%- include('../partials/footer') %>
