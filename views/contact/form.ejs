<%- include('../partials/header') %>

<section class="contact-hero">
  <div class="contact-overlay">
    <div class="contact-container">
      <span class="contact-badge">Get In Touch</span>
      <h1>Contact Us</h1>
      <p><a href="/">Home</a> / Contact Us</p>
    </div>
  </div>
</section>

<section class="contact-main-section">
  <div class="contact-container">

    <div class="contact-heading">
      <span class="section-tag">We Are Here To Help</span>
      <h2>Connect With Gyan Jyoti School</h2>
      <p>
        Have questions about admission, academics, fees, events or school facilities?
        Send us a message and our team will guide you.
      </p>
    </div>

    <div class="contact-grid">

      <div class="contact-info-card">
        <h3>Contact Information</h3>
        <p class="intro-text">Reach out to us through phone, email or visit our school office.</p>

        <div class="info-item">
          <div class="info-icon"><i class="fas fa-map-marker-alt"></i></div>
          <div>
            <h4>Address</h4>
            <p>
              <%= typeof schoolInfo !== 'undefined' && schoolInfo && schoolInfo.address ? schoolInfo.address : 'C4M4+858, Ruchika Catering Rd, North Civil Lines, Nainpur, Madhya Pradesh 481776' %>
            </p>
          </div>
        </div>

        <div class="info-item">
          <div class="info-icon"><i class="fas fa-phone-alt"></i></div>
          <div>
            <h4>Phone</h4>
            <p>
              <%= typeof schoolInfo !== 'undefined' && schoolInfo && schoolInfo.phone ? schoolInfo.phone : '+91 98765-43210' %>
            </p>
          </div>
        </div>

        <div class="info-item">
          <div class="info-icon"><i class="fas fa-envelope"></i></div>
          <div>
            <h4>Email</h4>
            <p>
              <%= typeof schoolInfo !== 'undefined' && schoolInfo && schoolInfo.email ? schoolInfo.email : 'info@gyanjyoti.com' %>
            </p>
          </div>
        </div>

        <div class="info-item">
          <div class="info-icon"><i class="fas fa-clock"></i></div>
          <div>
            <h4>Office Hours</h4>
            <p>Monday - Friday: 8:00 AM - 4:00 PM<br>Saturday: 9:00 AM - 1:00 PM</p>
          </div>
        </div>

        <div class="social-box">
          <h4>Follow Us</h4>
          <div class="social-icons">
            <a href="#" aria-label="Facebook"><i class="fab fa-facebook-f"></i></a>
            <a href="#" aria-label="Instagram"><i class="fab fa-instagram"></i></a>
            <a href="#" aria-label="YouTube"><i class="fab fa-youtube"></i></a>
            <a href="#" aria-label="WhatsApp"><i class="fab fa-whatsapp"></i></a>
          </div>
        </div>
      </div>

      <div class="contact-form-card">
        <h3>Send Us a Message</h3>
        <p>Fill out the form below and our team will contact you shortly.</p>

        <% if (typeof success !== 'undefined' && success) { %>
          <div class="success-msg">
            <i class="fas fa-check-circle"></i>
            Your message has been sent successfully.
          </div>
        <% } %>

        <% if (typeof error !== 'undefined' && error) { %>
          <div class="error-msg">
            <i class="fas fa-exclamation-circle"></i>
            Something went wrong. Please fill all details correctly.
          </div>
        <% } %>

        <form action="/contact" method="POST" class="secure-contact-form" novalidate>

          <div class="form-row">
            <div class="form-group">
              <label>Full Name <span>*</span></label>
              <input
                type="text"
                name="name"
                placeholder="Enter your full name"
                pattern="[A-Za-z\s.]{3,60}"
                maxlength="60"
                title="Only letters allowed, minimum 3 characters"
                autocomplete="name"
                required
              >
            </div>

            <div class="form-group">
              <label>Email Address <span>*</span></label>
              <input
                type="email"
                name="email"
                placeholder="your@email.com"
                maxlength="80"
                autocomplete="email"
                required
              >
            </div>
          </div>

          <div class="form-row">
            <div class="form-group">
              <label>Phone Number <span>*</span></label>
              <input
                type="text"
                name="phone"
                placeholder="10 digit mobile number"
                pattern="[0-9]{10}"
                maxlength="10"
                inputmode="numeric"
                title="Enter exactly 10 digits"
                autocomplete="tel"
                required
              >
            </div>

            <div class="form-group">
              <label>Category <span>*</span></label>
              <select name="category" required>
                <option value="">Select Category</option>
                <option value="Admission">Admission</option>
                <option value="Academics">Academics</option>
                <option value="Fees">Fees</option>
                <option value="Transport">Transport</option>
                <option value="Principal Message">Principal Message</option>
                <option value="General">General</option>
              </select>
            </div>
          </div>

          <div class="form-group">
            <label>Subject <span>*</span></label>
            <input
              type="text"
              name="subject"
              placeholder="Subject of your message"
              minlength="3"
              maxlength="120"
              required
            >
          </div>

          <div class="form-group">
            <label>Message <span>*</span></label>
            <textarea
              name="message"
              placeholder="Write your message here..."
              minlength="5"
              maxlength="1000"
              required
            ></textarea>
            <small class="char-note">
              Max 1000 characters. Scripts and unsafe symbols are not allowed.
            </small>
          </div>

          <button type="submit" class="submit-btn">
            <i class="fas fa-paper-plane"></i> Send Message
          </button>

        </form>
      </div>

    </div>

  </div>
</section>

<section class="connect-section">
  <div class="contact-container">

    <div class="connect-card">

      <div class="connect-left">
        <span class="section-tag">Visit & Connect</span>
        <h2>Start Your Journey With Gyan Jyoti</h2>
        <p>
          Admission enquiry, school visit, parent meeting or general support —
          our team is ready to guide you with the right information.
        </p>

        <div class="connect-actions">
          <a href="/admission/enquiry"><i class="fas fa-paper-plane"></i> Admission Enquiry</a>
          <a href="/admission/registration"><i class="fas fa-user-plus"></i> Register Now</a>
          <a href="tel:+919876543210"><i class="fas fa-phone"></i> Call School</a>
        </div>
      </div>

      <div class="connect-right">
        <div class="mini-card">
          <i class="fas fa-clock"></i>
          <h3>Office Hours</h3>
          <p>Mon - Fri: 8:00 AM - 4:00 PM<br>Sat: 9:00 AM - 1:00 PM</p>
        </div>

        <div class="mini-card">
          <i class="fas fa-location-dot"></i>
          <h3>Campus Visit</h3>
          <p>Parents can visit the school office for admission guidance and support.</p>
        </div>

        <div class="mini-card">
          <i class="fas fa-headset"></i>
          <h3>Quick Support</h3>
          <p>Our team responds to admission, contact and enquiry messages quickly.</p>
        </div>
      </div>

    </div>

  </div>
</section>

<style>
/* =========================================================
   GYAN JYOTI — PREMIUM CONTACT PAGE
   Navy / Gold luxury academic theme
   Backend routes + EJS variables preserved
========================================================= */

.contact-container{
  width:min(1220px,92%);
  margin:0 auto;
}

/* ========================= HERO ========================= */

.contact-hero{
  min-height:600px;
  position:relative;
  display:flex;
  align-items:center;
  overflow:hidden;
  background:#000b18;
}

.contact-hero::before{
  content:"";
  position:absolute;
  inset:0;
  background:
    linear-gradient(115deg,rgba(0,8,18,.94),rgba(0,31,63,.78) 58%,rgba(0,31,63,.55)),
    url('/images/school-bg.jpg') center/cover no-repeat;
}

.contact-hero::after{
  content:"";
  position:absolute;
  inset:0;
  opacity:.22;
  background-image:
    linear-gradient(rgba(255,255,255,.10) 1px,transparent 1px),
    linear-gradient(90deg,rgba(255,255,255,.10) 1px,transparent 1px);
  background-size:72px 72px;
  animation:contactGrid 28s linear infinite;
}

@keyframes contactGrid{
  from{transform:translate(0,0)}
  to{transform:translate(-72px,-72px)}
}

.contact-overlay{
  position:relative;
  z-index:2;
  width:100%;
  padding:95px 0;
}

.contact-hero .contact-container{
  position:relative;
}

.contact-hero .contact-container::after{
  content:"";
  position:absolute;
  width:430px;
  height:430px;
  right:-130px;
  top:-180px;
  border:1px solid rgba(255,195,0,.18);
  border-radius:50%;
  box-shadow:
    0 0 0 60px rgba(255,195,0,.025),
    0 0 0 120px rgba(255,195,0,.018);
  animation:heroRing 8s ease-in-out infinite;
}

@keyframes heroRing{
  0%,100%{transform:translateY(0) rotate(0deg)}
  50%{transform:translateY(-16px) rotate(5deg)}
}

.contact-badge,
.section-tag{
  display:inline-flex;
  align-items:center;
  gap:8px;
  padding:10px 20px;
  border-radius:50px;
  background:#ffc300;
  color:#001f3f;
  font-size:10px;
  font-weight:950;
  letter-spacing:1.4px;
  text-transform:uppercase;
  box-shadow:0 14px 32px rgba(255,195,0,.18);
}

.contact-hero h1{
  position:relative;
  z-index:2;
  max-width:900px;
  margin:24px 0 16px;
  color:#fff;
  font-size:clamp(56px,8vw,92px);
  line-height:.94;
  letter-spacing:-4px;
  font-weight:950;
  text-shadow:0 22px 50px rgba(0,0,0,.42);
}

.contact-hero h1::after{
  content:"";
  display:block;
  width:90px;
  height:5px;
  margin-top:23px;
  border-radius:10px;
  background:#ffc300;
  box-shadow:0 5px 20px rgba(255,195,0,.35);
}

.contact-hero p{
  position:relative;
  z-index:3;
  display:flex;
  align-items:center;
  gap:10px;
  margin:0;
  color:rgba(255,255,255,.65);
  font-size:14px;
  font-weight:650;
}

.contact-hero p a{
  color:#ffc300;
  text-decoration:none;
  transition:.3s;
}

.contact-hero p a:hover{
  color:#fff;
}

/* ========================= MAIN ========================= */

.contact-main-section{
  padding:110px 0;
  background:
    radial-gradient(circle at 0 0,rgba(255,195,0,.07),transparent 25%),
    #f5f8fb;
}

.contact-heading{
  max-width:850px;
  margin:0 auto 58px;
  text-align:center;
}

.contact-heading h2{
  margin:18px 0 13px;
  color:#001f3f;
  font-size:clamp(40px,5vw,58px);
  line-height:1.03;
  letter-spacing:-2.5px;
  font-weight:950;
}

.contact-heading p{
  max-width:760px;
  margin:auto;
  color:#6b7a87;
  font-size:16px;
  line-height:1.85;
}

/* ========================= GRID ========================= */

.contact-grid{
  display:grid;
  grid-template-columns:.82fr 1.18fr;
  gap:25px;
  align-items:stretch;
}

.contact-info-card,
.contact-form-card{
  position:relative;
  overflow:hidden;
  border-radius:32px;
  box-shadow:0 28px 70px rgba(0,31,63,.12);
}

.contact-info-card{
  padding:43px;
  background:
    radial-gradient(circle at 100% 0,rgba(255,195,0,.17),transparent 31%),
    linear-gradient(145deg,#000a16,#001f3f 62%,#004481);
  color:#fff;
}

.contact-info-card::before{
  content:"";
  position:absolute;
  width:310px;
  height:310px;
  right:-190px;
  bottom:-190px;
  border:1px solid rgba(255,195,0,.15);
  border-radius:50%;
  box-shadow:0 0 0 50px rgba(255,195,0,.025);
}

.contact-info-card::after,
.contact-form-card::after{
  content:"";
  position:absolute;
  width:240px;
  height:240px;
  top:-125px;
  right:-125px;
  border-radius:50%;
  background:rgba(255,195,0,.08);
}

.contact-info-card > *,
.contact-form-card > *{
  position:relative;
  z-index:2;
}

.contact-info-card h3{
  color:#fff;
  font-size:29px;
  line-height:1.1;
  font-weight:950;
  margin-bottom:10px;
}

.intro-text{
  max-width:430px;
  margin-bottom:29px;
  color:rgba(255,255,255,.61);
  font-size:13px;
  line-height:1.8;
}

.info-item{
  display:flex;
  gap:14px;
  align-items:flex-start;
  margin-bottom:13px;
  padding:17px;
  border:1px solid rgba(255,255,255,.08);
  border-left:3px solid #ffc300;
  border-radius:17px;
  background:rgba(255,255,255,.055);
  transition:.35s;
}

.info-item:hover{
  transform:translateX(6px);
  background:rgba(255,195,0,.075);
  border-color:rgba(255,195,0,.22);
}

.info-icon{
  flex:0 0 43px;
  width:43px;
  height:43px;
  display:flex;
  align-items:center;
  justify-content:center;
  border-radius:13px;
  background:rgba(255,195,0,.13);
  color:#ffc300;
  font-size:17px;
}

.info-item h4{
  margin:0 0 4px;
  color:#fff;
  font-size:14px;
  font-weight:900;
}

.info-item p{
  margin:0;
  color:rgba(255,255,255,.57);
  font-size:11px;
  line-height:1.65;
}

.social-box{
  margin-top:27px;
  padding-top:24px;
  border-top:1px solid rgba(255,255,255,.10);
}

.social-box h4{
  margin-bottom:13px;
  color:#fff;
  font-size:11px;
  letter-spacing:.5px;
}

.social-icons{
  display:flex;
  gap:9px;
}

.social-icons a{
  width:42px;
  height:42px;
  display:flex;
  align-items:center;
  justify-content:center;
  border-radius:12px;
  background:rgba(255,255,255,.08);
  border:1px solid rgba(255,255,255,.10);
  color:#ffc300;
  text-decoration:none;
  transition:.35s;
}

.social-icons a:hover{
  transform:translateY(-6px);
  background:#ffc300;
  color:#001f3f;
}

/* ========================= FORM ========================= */

.contact-form-card{
  padding:43px;
  background:#fff;
}

.contact-form-card::before{
  content:"CONTACT DESK";
  position:absolute;
  top:26px;
  right:32px;
  color:#dce4ea;
  font-size:8px;
  font-weight:950;
  letter-spacing:2px;
}

.contact-form-card h3{
  margin:0 0 7px;
  color:#001f3f;
  font-size:29px;
  font-weight:950;
}

.contact-form-card > p{
  margin:0 0 29px;
  color:#81909b;
  font-size:13px;
  line-height:1.7;
}

.form-row{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:15px;
}

.form-group{
  margin-bottom:17px;
}

.form-group label{
  display:block;
  margin-bottom:7px;
  color:#001f3f;
  font-size:11px;
  font-weight:900;
}

.form-group span{
  color:#dc2626;
}

.contact-form-card input,
.contact-form-card select,
.contact-form-card textarea{
  width:100%;
  border:1px solid #dce4ea;
  border-radius:13px;
  outline:none;
  background:#fbfcfd;
  color:#001f3f;
  font-family:inherit;
  font-size:13px;
  transition:.3s;
}

.contact-form-card input,
.contact-form-card select{
  height:49px;
  padding:0 15px;
}

.contact-form-card textarea{
  min-height:145px;
  padding:14px 15px;
  resize:vertical;
}

.contact-form-card input:focus,
.contact-form-card select:focus,
.contact-form-card textarea:focus{
  border-color:#ffc300;
  background:#fff;
  box-shadow:0 0 0 4px rgba(255,195,0,.12);
}

.contact-form-card input::placeholder,
.contact-form-card textarea::placeholder{
  color:#a3afb8;
}

.char-note{
  display:flex;
  justify-content:space-between;
  margin-top:7px;
  color:#98a4ad;
  font-size:9px;
}

.submit-btn{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:10px;
  width:100%;
  min-height:54px;
  border:0;
  border-radius:15px;
  background:linear-gradient(135deg,#ffc300,#ffdc4d);
  color:#001f3f;
  font-family:inherit;
  font-size:13px;
  font-weight:950;
  cursor:pointer;
  box-shadow:0 15px 32px rgba(255,195,0,.25);
  transition:.35s;
}

.submit-btn:hover{
  transform:translateY(-4px);
  box-shadow:0 22px 42px rgba(255,195,0,.32);
}

.submit-btn:disabled{
  opacity:.7;
  cursor:not-allowed;
  transform:none;
}

.success-msg,
.error-msg{
  display:flex;
  align-items:center;
  gap:10px;
  padding:14px 15px;
  border-radius:14px;
  margin-bottom:19px;
  font-size:11px;
  font-weight:850;
}

.success-msg{
  background:#effaf4;
  border:1px solid #c9eedb;
  color:#126b40;
}

.error-msg{
  background:#fff1f2;
  border:1px solid #fecdd3;
  color:#9f1239;
}

/* ========================= CONNECT ========================= */

.connect-section{
  position:relative;
  overflow:hidden;
  padding:105px 0;
  background:
    linear-gradient(120deg,rgba(0,8,18,.97),rgba(0,31,63,.92)),
    url('/images/school-bg.jpg') center/cover no-repeat;
}

.connect-section::after{
  content:"";
  position:absolute;
  width:500px;
  height:500px;
  right:-260px;
  top:-280px;
  border:1px solid rgba(255,195,0,.14);
  border-radius:50%;
  box-shadow:0 0 0 70px rgba(255,195,0,.02);
}

.connect-card{
  position:relative;
  z-index:2;
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:45px;
  align-items:center;
}

.connect-left{
  color:#fff;
}

.connect-left h2{
  max-width:620px;
  margin:19px 0 15px;
  color:#fff;
  font-size:clamp(42px,5vw,60px);
  line-height:1.02;
  letter-spacing:-2px;
  font-weight:950;
}

.connect-left p{
  max-width:610px;
  margin-bottom:27px;
  color:rgba(255,255,255,.61);
  font-size:14px;
  line-height:1.85;
}

.connect-actions{
  display:flex;
  flex-wrap:wrap;
  gap:10px;
}

.connect-actions a{
  display:inline-flex;
  align-items:center;
  gap:8px;
  padding:14px 19px;
  border-radius:50px;
  background:#ffc300;
  color:#001f3f;
  text-decoration:none;
  font-size:10px;
  font-weight:950;
  box-shadow:0 12px 27px rgba(255,195,0,.20);
  transition:.35s;
}

.connect-actions a:hover{
  transform:translateY(-5px);
  background:#fff;
}

.connect-right{
  display:grid;
  gap:12px;
}

.mini-card{
  display:grid;
  grid-template-columns:50px 1fr;
  column-gap:15px;
  padding:20px;
  border:1px solid rgba(255,255,255,.12);
  border-radius:19px;
  background:rgba(255,255,255,.065);
  backdrop-filter:blur(14px);
  color:#fff;
  transition:.35s;
}

.mini-card:hover{
  transform:translateX(-8px);
  border-color:rgba(255,195,0,.35);
  background:rgba(255,255,255,.10);
}

.mini-card i{
  grid-row:span 2;
  width:50px;
  height:50px;
  display:flex;
  align-items:center;
  justify-content:center;
  border-radius:14px;
  background:rgba(255,195,0,.10);
  color:#ffc300;
  font-size:20px;
}

.mini-card h3{
  align-self:end;
  margin:0 0 4px;
  color:#fff;
  font-size:14px;
  font-weight:900;
}

.mini-card p{
  align-self:start;
  margin:0;
  color:rgba(255,255,255,.52);
  font-size:10px;
  line-height:1.65;
}

/* ========================= RESPONSIVE ========================= */

@media(max-width:900px){
  .contact-grid,
  .form-row,
  .connect-card{
    grid-template-columns:1fr;
  }

  .contact-hero{
    min-height:540px;
  }

  .contact-hero h1{
    font-size:53px;
    letter-spacing:-2.5px;
  }

  .contact-heading h2{
    font-size:40px;
  }

  .connect-left h2{
    font-size:42px;
  }
}

@media(max-width:600px){
  .contact-container{
    width:92%;
  }

  .contact-main-section,
  .connect-section{
    padding:78px 0;
  }

  .contact-overlay{
    padding:70px 0;
  }

  .contact-hero h1{
    font-size:45px;
  }

  .contact-hero .contact-container::after{
    right:-250px;
    top:-120px;
  }

  .contact-info-card,
  .contact-form-card{
    padding:27px 21px;
    border-radius:24px;
  }

  .contact-form-card::before{
    display:none;
  }

  .contact-heading h2{
    font-size:36px;
  }

  .connect-left h2{
    font-size:38px;
  }

  .connect-actions{
    flex-direction:column;
  }

  .connect-actions a{
    justify-content:center;
  }

  .mini-card{
    grid-template-columns:44px 1fr;
  }

  .mini-card i{
    width:44px;
    height:44px;
  }
}

@media(prefers-reduced-motion:reduce){
  .contact-hero::after,
  .contact-hero .contact-container::after{
    animation:none;
  }

  *{
    scroll-behavior:auto !important;
  }
}
</style>


<script>
  const contactForm = document.querySelector('.secure-contact-form');

  if (contactForm) {
    contactForm.addEventListener('submit', function(e) {
      const name = this.name.value.trim();
      const email = this.email.value.trim();
      const phone = this.phone.value.trim();
      const category = this.category.value.trim();
      const subject = this.subject.value.trim();
      const message = this.message.value.trim();

      const nameRegex = /^[A-Za-z\s.]{3,60}$/;
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      const unsafeRegex = /<script|<\/script|javascript:|onerror=|onload=|<iframe|<\/iframe/gi;

      if (!nameRegex.test(name)) {
        alert('Full name should contain only letters and minimum 3 characters.');
        e.preventDefault();
        return;
      }

      if (!emailRegex.test(email)) {
        alert('Please enter a valid email address.');
        e.preventDefault();
        return;
      }

      if (!/^[0-9]{10}$/.test(phone)) {
        alert('Phone number must be exactly 10 digits.');
        e.preventDefault();
        return;
      }

      if (!category) {
        alert('Please select a category.');
        e.preventDefault();
        return;
      }

      if (subject.length < 3 || subject.length > 120 || unsafeRegex.test(subject)) {
        alert('Please enter a valid subject.');
        e.preventDefault();
        return;
      }

      if (message.length < 5 || message.length > 1000 || unsafeRegex.test(message)) {
        alert('Please enter a valid message. Unsafe script text is not allowed.');
        e.preventDefault();
        return;
      }

      const btn = this.querySelector('.submit-btn');
      btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Sending...';
      btn.disabled = true;
    });
  }
</script>



<%- include('../partials/footer') %>